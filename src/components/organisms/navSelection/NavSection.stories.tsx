import { ThemeProvider } from "@emotion/react";
;
import NavSelection from "./NavSelection";
export default{
    title:'organisms/NavSelection',
    component:NavSelection
}
const Template=()=> <NavSelection />;
export const NavSection= Template.bind({});
